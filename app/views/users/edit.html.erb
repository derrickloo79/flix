<h1>Editing <%= @user.name %>'s profile</h1>

<%= render "form", user: @user %>

<%= link_to "Cancel", user_path, class: "btn btn-outline-primary", style: "width: 100%; max-width: 503px; margin-top: 8px" %>