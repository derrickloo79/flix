<%= form_with(model: [movie, review]) do |r| %>

    <%= render "shared/errors", object: review %>

    <%= r.label :stars %>
    <%= r.select :stars, Review::STARS, prompt: "Pick one" %>

    <%= r.label :comment %>
    <%= r.text_area :comment, rows: 7 %>

    <%= r.submit %>
<% end %>