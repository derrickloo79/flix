<h1>Editing <%= @movie.title %></h1>

<%= render "form", movie: @movie %>

<%= link_to "Cancel", movie_path, class: "btn btn-outline-primary", style: "width: 100%; max-width: 503px; margin-top: 8px" %>